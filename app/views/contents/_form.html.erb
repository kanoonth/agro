<%= form_for(@content) do |f| %>
  <% if @content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content.errors.count, "error") %> prohibited this content from being saved:</h2>

      <ul>
      <% @content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="field">
    <%= f.label :content_image %><br>
    <%= f.file_field :content_image %>
  </div>
  <div class="field">
    <%= f.label :content_type_id %><br>
    <%= collection_select :content, :content_type_id, @content_types, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :disease_id %><br>
    <%= collection_select :content, :disease_id, @diseases, :id, :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
