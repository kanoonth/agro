<%= form_for(@region_certainty_factor) do |f| %>
  <% if @region_certainty_factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@region_certainty_factor.errors.count, "error") %> prohibited this region_certainty_factor from being saved:</h2>

      <ul>
      <% @region_certainty_factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :region_id %><br>
    <%= f.text_field :region_id %>
  </div>
  <div class="field">
    <%= f.label :disease_id %><br>
    <%= f.text_field :disease_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
