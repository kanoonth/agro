<%= form_for(@stage) do |f| %>
  <% if @stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

      <ul>
      <% @stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :min_day %><br>
    <%= f.number_field :min_day %>
  </div>
  <div class="field">
    <%= f.label :max_day %><br>
    <%= f.number_field :max_day %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
