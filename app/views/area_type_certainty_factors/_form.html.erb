<%= form_for(@area_type_certainty_factor) do |f| %>
  <% if @area_type_certainty_factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area_type_certainty_factor.errors.count, "error") %> prohibited this area_type_certainty_factor from being saved:</h2>

      <ul>
      <% @area_type_certainty_factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :disease_id %><br>
    <%= f.text_field :disease_id %>
  </div>
  <div class="field">
    <%= f.label :area_type_id %><br>
    <%= f.text_field :area_type_id %>
  </div>
  <div class="field">
    <%= f.label :cf %><br>
    <%= f.text_field :cf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
